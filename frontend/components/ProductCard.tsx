import { Product } from "./types";
const rupees = (value: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value);
const icon = (category: string) => ({ audio: "◖◗", wearables: "◌", accessories: "⌁", work: "⌘" }[category] || "✦");
export function ProductCard({ product, onAdd }: { product: Product; onAdd: (product: Product) => void }) { return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:border-[#4b6bfb]/35 hover:shadow-lg"><div className="mb-4 flex items-start justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#eef1ff] text-xl font-bold text-[#4b6bfb]">{icon(product.category)}</div><span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-semibold text-emerald-700">In stock</span></div><p className="text-[11px] font-semibold uppercase tracking-wider text-[#6471cf]">{product.category}</p><h3 className="mt-1 font-semibold text-[#18214d]">{product.name}</h3><p className="mt-2 min-h-10 text-xs leading-5 text-slate-500">{product.description}</p><div className="mt-4 flex items-center justify-between"><span className="text-base font-bold text-[#18214d]">{rupees(product.price)}</span><button onClick={() => onAdd(product)} className="rounded-lg bg-[#3156e8] px-3 py-2 text-xs font-semibold text-white">Add to cart</button></div></article>; }
