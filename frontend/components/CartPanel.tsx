import { Product } from "./types";
const rupees = (value: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value);
export function CartPanel({ cart, onCheckout }: { cart: Product[]; onCheckout: () => void }) { const total = cart.reduce((sum, item) => sum + (item.line_total ?? item.price * (item.quantity ?? 1)), 0); return <aside className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-[#18214d]">Your cart</h2><p className="mt-0.5 text-xs text-slate-500">{cart.length ? `${cart.length} product${cart.length > 1 ? "s" : ""} selected` : "Ready when you are"}</p></div><div className="rounded-full bg-[#eef1ff] px-2.5 py-1 text-xs font-bold text-[#3156e8]">{cart.length}</div></div>{!cart.length ? <div className="py-9 text-center"><div className="mx-auto flex h-10 w-10 items-center justify-center rounded-full bg-slate-100 text-slate-400">+</div><p className="mt-3 text-sm text-slate-500">Add products as you browse.</p></div> : <><div className="mt-5 space-y-4">{cart.map(item => <div className="flex gap-3 text-sm" key={item.id}><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-[#eef1ff] font-bold text-[#4b6bfb]">{item.name[0]}</div><div className="min-w-0 flex-1"><p className="truncate font-medium text-[#18214d]">{item.name}</p><p className="mt-0.5 text-xs text-slate-500">Qty {item.quantity ?? 1}</p></div><b className="text-[#18214d]">{rupees(item.line_total ?? item.price)}</b></div>)}</div><div className="mt-5 border-t border-dashed border-slate-200 pt-4"><div className="flex justify-between text-sm font-semibold text-[#18214d]"><span>Total payable</span><span>{rupees(total)}</span></div><p className="mt-1 text-[11px] text-slate-500">Final amount verified before payment</p></div><button onClick={onCheckout} className="mt-4 w-full rounded-xl bg-[#3156e8] py-3 text-sm font-semibold text-white shadow-lg shadow-[#3156e8]/20">Continue to checkout →</button></>}</aside>; }
