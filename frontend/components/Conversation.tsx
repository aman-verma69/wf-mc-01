import { ProductCard } from "./ProductCard";
import { Message, Product } from "./types";
export function Conversation({ messages, onAdd }: { messages: Message[]; onAdd: (product: Product) => void }) { return <div className="space-y-6">{messages.map((message, index) => <div key={index} className={message.role === "user" ? "ml-auto max-w-[82%]" : "max-w-[92%]"}>{message.role === "agent" && <p className="mb-2 text-xs font-semibold text-[#6471cf]">SHOPPING ASSISTANT</p>}<div className={`rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === "user" ? "rounded-br-md bg-[#3156e8] text-white" : message.blocked ? "border border-rose-200 bg-rose-50 text-rose-800" : "border border-slate-100 bg-[#f7f8ff] text-[#26315c]"}`}>{message.text}</div>{message.products?.length ? <div className="mt-3 grid gap-3 sm:grid-cols-2">{message.products.map(product => <ProductCard key={product.id} product={product} onAdd={onAdd} />)}</div> : null}</div>)}</div>; }
