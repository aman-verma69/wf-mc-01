"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Button, Input, StateMessage } from "@/components/ui";
import { useAuth } from "@/lib/auth";
const API = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000/api/v1";
export default function NewProductPage() { const router = useRouter(); const { customer } = useAuth(); const [form, setForm] = useState({ sku: "", name: "", description: "", price_paise: "", initial_stock: "" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setLoading(true); try { const token = localStorage.getItem("fieldhouse_token"); const response = await fetch(`${API}/products`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify({ ...form, price_paise: Number(form.price_paise), initial_stock: Number(form.initial_stock || 0) }) }); if (!response.ok) throw new Error((await response.json()).detail || "Unable to create product"); router.push("/merchant"); } catch (err) { setError(err instanceof Error ? err.message : "Unable to create product"); } finally { setLoading(false); } } if (customer?.role !== "merchant") return <div className="mx-auto max-w-2xl px-5 py-16"><StateMessage>Merchant access is required.</StateMessage></div>; return <div className="mx-auto max-w-2xl px-5 py-10 lg:py-16"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--primary)]">Catalog</p><h1 className="mt-3 font-display text-4xl font-bold">Add a product</h1><form onSubmit={submit} className="mt-10 space-y-5"><label className="block text-sm font-semibold">Product name<Input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} className="mt-2" /></label><label className="block text-sm font-semibold">SKU<Input required value={form.sku} onChange={e => setForm({ ...form, sku: e.target.value })} className="mt-2" /></label><label className="block text-sm font-semibold">Description<textarea value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} className="mt-2 min-h-28 w-full resize-y border border-[var(--border)] bg-white p-3 text-sm outline-none focus:border-[var(--primary)]" /></label><div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-semibold">Price (paise)<Input required type="number" min="0" value={form.price_paise} onChange={e => setForm({ ...form, price_paise: e.target.value })} className="mt-2" /></label><label className="block text-sm font-semibold">Initial stock<Input type="number" min="0" value={form.initial_stock} onChange={e => setForm({ ...form, initial_stock: e.target.value })} className="mt-2" /></label></div>{error && <p role="alert" className="text-sm text-[var(--destructive)]">{error}</p>}<Button loading={loading}>Create product</Button></form></div>; }
